#pragma once

// ✅ 地球半径（单位球渲染）
constexpr double EARTH_RADIUS = 1.0;

// ✅ 飞机高度放大系数（视觉增强用）
constexpr double HEIGHT_EXAGGERATION = 1.0;

// ✅ 飞机模型缩放比例（适配单位球）
constexpr float AIRCRAFT_SCALE = 0.01f;

// ✅ 摄像机默认距离（建议初始）
constexpr float CAMERA_DEFAULT_DISTANCE = 1.5f;
